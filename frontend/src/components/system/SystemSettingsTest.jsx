import React from 'react';
import { Typography, Box } from '@mui/material';

const SystemSettingsTest = () => {
  return (
    <Box>
      <Typography variant="h4">System Settings Test</Typography>
      <Typography>This is a test component to verify imports work.</Typography>
    </Box>
  );
};

export default SystemSettingsTest;